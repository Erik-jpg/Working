const h1 = (text, style) => `<h1 style{${style}}>${text}</h1>;`

const app = () => {
  const rootEl = document.getElementById('root');
  const HellWorldH1 = ;

  rootEl.innerHTML += HelloWorldH1;
}

app();